from Evidence import Evidence

class DempsterRule:
    def __init__(self, ev1, ev2):
        self.ev1 = ev1
        self.ev2 = ev2
        self.evout = Evidence(None, None)

        for x in self.bm1.get_entries():
            for y in self.bm2.get_entries():
                if x[0] or y[0] is 'omega':
                    prob = 
                    # set bmout value equals the other one
                if x and y is empty:
                    # calc
                else:
                    unify and do stuff

        # bmout.addentry